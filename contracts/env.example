# Hardhat Configuration - Celo Mainnet Deployment
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it's in .gitignore

# Private key of the deployer wallet (without 0x prefix)
# ⚠️  SECURITY WARNING: Keep this secret! Never share or commit this value.
PRIVATE_KEY=your_wallet_private_key_here

# VictoryVault uses native CELO for staking (no ERC20 token required)
# Users stake native CELO directly via msg.value

# Address that receives platform fees
# If not set, defaults to deployer address
# ⚠️  Must be a valid address (cannot be zero address)
FEE_RECIPIENT=0x0000000000000000000000000000000000000000

# Platform fee in basis points (200 = 2%, 1000 = 10%, max 1000 = 10%)
# Default: 200 (2%)
# Maximum allowed: 1000 (10%)
PLATFORM_FEE_BPS=200

# Network-specific RPC URLs (optional, defaults to public RPCs)
# Celo Mainnet RPC: https://forno.celo.org
CELO_RPC_URL=https://forno.celo.org

